import { Mode as $Mode } from "./mode.types";
import { Flags as $Flags } from "./flags.types";

export declare namespace ZuordCore {
    export import Mode = $Mode;
    export import Flags = $Flags;
}