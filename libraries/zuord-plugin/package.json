{
  "name": "zuord-plugin",
  "version": "0.0.23",
  "main": "index.js",
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "npx esbuild src/init.ts --bundle --platform=node --external:vscode --outfile=index.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}