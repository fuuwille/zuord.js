import fs from "fs";
import path from "path";
import { Project, SourceFile } from "ts-morph";
import { FileType } from "./file";
import { File, ModuleFileD } from "./file";
import { member as zmember } from "./member";
import { node as znode } from "./node";

export const initialize = (
    sourceFile: SourceFile, mode: FileType
) : ModuleFileD => {

    const moduleFile : ModuleFileD = {
        source: sourceFile,
        type: mode,
        members: [],
        discarded: [],
        others: []
    };

    sourceFile.forEachChild((node) => {
        const member = zmember.create(node);

        if(znode.isKnownLike(node)) {
            const collection = znode.isDiscardedBy(node, mode)
                ? moduleFile.discarded
                : moduleFile.members;

            collection.push(member);
        }
        else {
            moduleFile.others.push(member);
        }
    });

    return moduleFile;
};

export const extract = (sourceFile: SourceFile, mode: FileType) : ModuleFileD => {
    switch(mode) {
        case FileType.Schema:
            return extractSchema(sourceFile);
        case FileType.Variants:
            return extractVariants(sourceFile);
        default:
            throw new Error(`Unknown module file kind: ${mode}`);
    }
};

export const extractAtPath = <TFile extends ModuleFileD>(dir: string, name: string, mode: FileType) : TFile | undefined => {
    const fileName = `${name}.${mode.toLowerCase()}.ts`;
    const filePath = path.join(dir, fileName);

    const sourceFile = new Project().addSourceFileAtPath(filePath);

    if(fs.existsSync(filePath)) {
        return extract(sourceFile, mode) as TFile;
    }

    return undefined;
};

export const extractSchema = (sourceFile: SourceFile) : File.Schema => {
    return initialize(sourceFile, FileType.Schema) as File.Schema;
};

export const extractVariants = (sourceFile: SourceFile) : File.Variants => {
    return initialize(sourceFile, FileType.Variants) as File.Variants;
};