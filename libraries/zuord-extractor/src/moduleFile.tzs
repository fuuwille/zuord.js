import { SourceFile } from "ts-morph";
import { ModuleFileMode } from "./moduleFile";
import { Member as ZMember } from "./member";

export interface Base {
    source: SourceFile
    mode: ModuleFileMode;
    members: ZMember.Base[];
    discarded: ZMember.Base[];
    others: ZMember.Base[];
};

export interface Schema extends ZSchema.Base {
    members: ZMember.SchemaLike[];
}

export interface Variants extends ZSchema.Base {
    members: ZMember.VariantLike[];
}