import { SourceFile } from "ts-morph";
import { ModuleFileMode } from "./moduleFile";
import { FileMember } from "./member";

export interface Base {
    source: SourceFile
    mode: ModuleFileMode;
    members: FileMember.Base[];
    discarded: FileMember.Base[];
    others: FileMember.Base[];
};

export interface Schema extends ZSchema.Base {
    members: FileMember.SchemaLike[];
}

export interface Variants extends ZSchema.Base {
    members: FileMember.VariantLike[];
}