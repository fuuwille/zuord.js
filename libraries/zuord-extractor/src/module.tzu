import { ModuleBundle } from './module';

export const getSchemaExportText = (bundle: ModuleBundle): string => {
    const names = bundle.content.schemas
        .map(schema => schema.name)
        .filter(Boolean);

    return `export { ${names.join(", ")} }`;
}