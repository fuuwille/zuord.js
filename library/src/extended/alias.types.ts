import { Integrate as IntegrateModule } from "./integrate";
import { Mode as ModeModule } from "./mode";

export declare namespace X {
    export import Integrate = IntegrateModule;
    export import Mode = ModeModule;
}