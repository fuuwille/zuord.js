import { Integrate as $Integrate } from "./integrate";
import { Merge as $Merge } from "./merge";
import { Evolve as $Evolve } from "./evolve";
import { Pick as $Pick } from "./pick";
import { Omit as $Omit } from "./omit";

export declare namespace ZuordModeX {
    export import Integrate = $Integrate;
    export import Merge = $Merge;
    export import Evolve = $Evolve;
    export import Pick = $Pick;
    export import Omit = $Omit;
}