import { Integrate as IntegrateModule } from "./integrate.types";
import { Merge as MergeModule } from "./merge.types";
import { Pick as PickModule } from "./pick.types";
import { Omit as OmitModule } from "./omit.types";
import { Mode as ModeModule } from "./mode.types";

export declare namespace InternalZuord {
    export import Integrate = IntegrateModule;
    export import Merge = MergeModule;
    export import Pick = PickModule;
    export import Omit = OmitModule;
    export import Mode = ModeModule;
}